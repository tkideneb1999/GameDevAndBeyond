#pragma once

#include <fstream>

#include "GL/glew.h"

namespace Shader
{
	void LoadShaderSource(const GLchar* const* outSource, const char* filePath)
	{
		std::ifstream fileStream(filePath);
		outsource
	}
}